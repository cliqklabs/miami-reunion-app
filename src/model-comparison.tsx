import React from 'react'
import ReactDOM from 'react-dom/client'
import ModelComparison from './components/ModelComparison'

ReactDOM.createRoot(document.getElementById('model-comparison-root')!).render(
  <React.StrictMode>
    <ModelComparison />
  </React.StrictMode>,
)
